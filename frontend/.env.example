# Development server proxy target. No frontend source-code change is needed when the API host or port changes.
VITE_BACKEND_URL=http://localhost:8080

# Optional production API base path. Keep /api when a reverse proxy routes /api to the backend.
VITE_API_BASE_URL=/api
